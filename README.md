# tracking_pipeline
this tracking pipeline uses alpha blended template update mechanism fused with opencv built in tracking pipeline, purpose for this pipeline is to provide realtime inference with allowing the pipeline capability to be adaptive and more resistant to variation of the target.
